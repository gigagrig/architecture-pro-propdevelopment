apiVersion: v1
kind: Pod
metadata:
  name: pod-root-user
  namespace: audit-zone
  labels:
    app: pod-root-user
spec:
  securityContext:
    runAsUser: 0
  containers:
    - name: nginx
      image: nginx:1.27-alpine
      securityContext:
        runAsNonRoot: false

